"""Test package marker — see ``conftest.py`` for the shared autouse
``_clear_fastgpt_metadata_cache`` fixture that resets the module-level
cache between cases (introduced in REWORK-PC2-B-INDEP C1)."""
